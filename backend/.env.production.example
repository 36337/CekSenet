# ÇekSenet Backend Environment Variables
# Production configuration
# Bu dosyayı .env.production olarak kopyalayın ve JWT_SECRET'i değiştirin!

# Server
PORT=7474
NODE_ENV=production

# JWT Secret - ÖNEMLİ: Bu değeri benzersiz ve güvenli bir değerle değiştirin!
# Örnek: openssl rand -base64 32 komutu ile oluşturabilirsiniz
JWT_SECRET=BURAYA-GUCLU-BIR-SECRET-YAZIN-EN-AZ-32-KARAKTER

# Database (varsayılan: ./database/ceksenet.db)
# DB_PATH=./database/ceksenet.db

# Logging (production'da info veya warn önerilir)
LOG_LEVEL=info
